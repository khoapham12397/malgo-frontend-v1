export enum Difficulty {
  easy = 'easy',
  medium = 'medium',
  hard = 'hard'
}

export enum ContestTableHeader {
  currentContest = 'Current Contest',
  upcomingContest = 'Upcoming Contest',
  pastContest = 'Past Contest'
}

export enum Role {
  regular_user = 'regular_user',
  admin = 'admin',
  super_admin = 'super_admin'
}
